# Config file for deploy
# Not necessarily safe on shared servers
# due to how the script uses the prod password (passing it on the command line).
# Use at own risk.

ERRORSTRING="Usage: deploy push or pull [go]"

# Production/Remote user name, server/domain name, database user name, database password, database name, location of files
PRODUSER='exampleuser'
PRODSERVER='example.com'
PRODDBUSER='exampleproddbuser'
PRODDBPASS='supersecret'
PRODDBNAME='exampledb'
PRODDIR='/var/www/'

# server/domain name, database user name, database password, database name, location of files
DEVSERVER='example.dev'
DEVDBUSER='exampledevdbuser'
DEVDBPASS='exampledbpass'
DEVDBNAME='exampledevdb'
DEVDIR='/var/www/'

# Database host used in search replace script, likely localhost or mysql.<domain>.com
DEVSQLHOST='localhost'
PRODSQLHOST='localhost'

# Files to exclude from syncing EXCLUDES is both ways
# PRODUCTIONEXCLUDES is to exclude additional files when syncing TO production
EXCLUDES='--exclude=wp-content/plugins/bwp-minify/cache/* --exclude=wp-content/cache'
PRODUCTIONEXCLUDES='--exclude=wp-config-local.php'

# Name of the search replace scripts
SEARCHREPLACE='searchreplacedb2cli.php'
SEARCHREPLACEINC='searchreplacedb2.php'
